function main()
{
	console.log(Pather.transform());

	// Pather.transform(text, variables, options);
	// options:
	// stripComments    -- strip out xml comments
	// stripWhitespace  -- strip out new lines, extra spaces in paths
	// optimisePaths    -- spit out optimal paths v/s keeping paths as defined

	return;
}
